<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.akeno0810.u22">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:name=".MyApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".EndAnqActivity"></activity>
        <activity android:name=".ShopAnqActivity" />
        <activity android:name=".DateAnqActivity" />
        <activity android:name=".StartAnqActivity" />
        <activity android:name=".GuestHomeActivity" />
        <activity android:name=".ModeActivity" />
        <activity android:name=".HostHomeActivity" />
        <activity android:name=".CreateUserActivity" />
        <activity android:name=".NewGroupActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>